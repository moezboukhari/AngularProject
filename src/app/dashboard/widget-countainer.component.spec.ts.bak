import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { WidgetCountainerComponent } from './widget-countainer.component';

describe('WidgetCountainerComponent', () => {
  let component: WidgetCountainerComponent;
  let fixture: ComponentFixture<WidgetCountainerComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ WidgetCountainerComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(WidgetCountainerComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
